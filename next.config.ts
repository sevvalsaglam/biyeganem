import type { NextConfig } from "next";

const nextConfig = {
  images: {
    domains: ["hebbkx1anhila5yf.public.blob.vercel-storage.com",'via.placeholder.com', 'res.cloudinary.com', 'images.unsplash.com'],
  },
}

export default nextConfig;
